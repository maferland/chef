---
description: Generate print-ready PDFs from a saved chef session — shopping list and day-of timeline
argument-hint: [session path or leave empty for most recent]
---

Invoke the `print` skill and follow it exactly as presented to you.

Arguments: $ARGUMENTS
