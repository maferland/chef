---
description: Publish a chef:plan session to mise. — maps the saved recipe to mise.'s ingest API and pushes it as a collection
argument-hint: [session path] [--dry-run]
---

Invoke the `publish` skill and follow it exactly as presented to you.

Arguments: $ARGUMENTS
